<?php
    $x = 5 /* + 15 */ + 5;
    // echo $x;
    $b = 10;
    $b**=$x;
    echo $x." ".$b;
    // + - * / % ** += -= *= /= %= ++ --
    // & | ^ ~(not) >> <<
    // == === (same datatype) != !== <> > < >= <= 
    // a <=> b return -1 if $a is less than $b, return 0 if $a is equal $b ,return 1 if $a is greater than $b
    // and or xor && || !
    // . .= (concatenations)
    // 
?>